id: skill_weaver_001
schema: 20_schemas/skill.schema.md
references: [SKILL_SYSTEM, SKILL_EFFECTS, SKILL_ANIMATION, STATUS_EFFECTS, ELEMENTS]
name: Firebolt                     # exact JOBS.md §4.1 first-job roster name (skill.schema rule 1)
line: weaver
tier: first                        # first-job kit, shared by all weaver specs, unlocks at Lv 8 (JOBS §1, SKILL_SYSTEM §2)
kind: active
targeting: { shape: projectile, speed: 12, range: 9, pierce: 0, gravity: false, impact_radius: 0 }   # roster target `projectile` (SKILL_SYSTEM §6)
cost: { essence: 5 }               # flat default; scaled per-rank below (first-job band 5->12, SKILL_SYSTEM §5)
cooldown: 2.5                      # flat default; scaled per-rank below (first-job band 0-8 s, core attacks 0-3 s, §5)
level_data:
  - level: 1
    essence_cost: 5
    cooldown: 2.5
    effects:
      - { op: deal_damage, element: fire, mult: 1.0 }   # scales on `spellpower` (staff default, SKILL_EFFECTS §1)
  - level: 4
    essence_cost: 7
    cooldown: 2.0
    effects:
      - { op: deal_damage, element: fire, mult: 1.2 }
  - level: 7
    essence_cost: 8
    cooldown: 1.5
    effects:
      - { op: deal_damage, element: fire, mult: 1.4 }
  - level: 10
    essence_cost: 9
    cooldown: 1.2
    effects:
      - { op: deal_damage, element: fire, mult: 1.6 }   # mult non-decreasing, cooldown non-increasing (§4 warn)
animation: skill_weaver_001_cast
flavor: "A stitched-together spark hurled at speed — the first fire every Weaver learns to throw before they learn to hold."
