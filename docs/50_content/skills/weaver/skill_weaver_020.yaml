id: skill_weaver_020
schema: 20_schemas/skill.schema.md
references: [SKILL_SYSTEM, SKILL_EFFECTS, SKILL_ANIMATION, STATUS_EFFECTS, ELEMENTS]
name: Combustion
line: weaver
tier: second
kind: passive
targeting: self
cost: { essence: 0 }
cooldown: 0
level_data:
  - level: 1
    effects:
      - { op: passive_stat_bonus, stats: { crit_rate: 0.03 }, mode: pct }
      - { op: on_hit_proc, trigger: on_crit, chance: 0.50, effect: { op: apply_status, status: burn, chance: 1.0 } }
  - level: 4
    effects:
      - { op: passive_stat_bonus, stats: { crit_rate: 0.05 }, mode: pct }
      - { op: on_hit_proc, trigger: on_crit, chance: 0.65, effect: { op: apply_status, status: burn, chance: 1.0 } }
  - level: 7
    effects:
      - { op: passive_stat_bonus, stats: { crit_rate: 0.07 }, mode: pct }
      - { op: on_hit_proc, trigger: on_crit, chance: 0.80, effect: { op: apply_status, status: burn, chance: 1.0 } }
  - level: 10
    effects:
      - { op: passive_stat_bonus, stats: { crit_rate: 0.09 }, mode: pct }
      - { op: on_hit_proc, trigger: on_crit, chance: 1.00, effect: { op: apply_status, status: burn, chance: 1.0 } }
flavor: "A crit-fed spark that catches anything the Cindercall lands a lucky hit on. Fortune and fire feed each other here."
