id: skill_bulwark_001
schema: 20_schemas/skill.schema.md
references: [SKILL_SYSTEM, SKILL_EFFECTS, SKILL_ANIMATION, STATUS_EFFECTS, ELEMENTS]
name: Shield Bash                  # exact JOBS.md §2.1 first-job roster name (skill.schema rule 1)
line: bulwark
tier: first                        # first-job kit, shared by all bulwark specs, unlocks at Lv 8 (JOBS §1, SKILL_SYSTEM §2)
kind: active
targeting: { shape: melee_arc, arc_degrees: 100, radius: 1.5 }   # roster target `melee_arc` (SKILL_SYSTEM §6)
cost: { essence: 5 }               # flat default; scaled per-rank below (first-job band 5->12, SKILL_SYSTEM §5)
cooldown: 3.0                      # flat default; scaled per-rank below (first-job band 0-8 s, core attacks 0-3 s, §5)
level_data:                        # rows only at 1/4/7/10; ranks 2/3/5/6/8/9 interpolate (SKILL_SYSTEM §4)
  - level: 1
    essence_cost: 5
    cooldown: 3.0
    effects:                       # order/element/op-list identical across rows (non-interpolatable)
      - { op: deal_damage, element: neutral, mult: 1.0 }              # scales on `power` (blade default, SKILL_EFFECTS §1)
      - { op: apply_status, status: stun, chance: 0.20 }               # STATUS_EFFECTS §4.1 hard CC, base dur 1.5 s
  - level: 4
    essence_cost: 7
    cooldown: 2.4
    effects:
      - { op: deal_damage, element: neutral, mult: 1.2 }
      - { op: apply_status, status: stun, chance: 0.25 }
  - level: 7
    essence_cost: 8
    cooldown: 1.8
    effects:
      - { op: deal_damage, element: neutral, mult: 1.4 }
      - { op: apply_status, status: stun, chance: 0.30 }
  - level: 10
    essence_cost: 9
    cooldown: 1.5
    effects:
      - { op: deal_damage, element: neutral, mult: 1.6 }               # mult non-decreasing, cooldown non-increasing (§4 warn)
      - { op: apply_status, status: stun, chance: 0.35 }
animation: skill_bulwark_001_cast
flavor: "A shield slammed into whatever's closest, hard enough to rattle teeth. Every Bulwark's first lesson: hit first, hit solid."
