id: mob_010
schema: 20_schemas/monster.schema.md
references: [COMBAT_FORMULA, STATUS_EFFECTS, AI_BEHAVIOR, DROPS, ELEMENTS, LEVELING]
name: Coalback Brute
tier: normal                     # ID slot 010 = last R1 normal (ID_REGISTRY Monsters)
element: fire                    # 3 of R1's 3 fire normals (WORLD_PLAN R1 mix)
weak_to: [frost]                 # a cinder-crusted brute (ELEMENTS §2; mutually exclusive lists)
resists: [fire]
level: 8
size_class: large
stats:                           # copied from COMBAT_FORMULA §13 normal budget, Lv 8
  life: 485                      # 4*(8+3)^2
  power: 30                      # 6+3*8 (touch dmg = round(0.5*power); fire body per element, §13.1)
  armor: 54                      # reallocated toward armor; armor+warding sum = 96 = 12*8 (§13)
  warding: 42                    # keeps the defense budget sum
  precision: 32                  # 4*8
  evasion: 3.2                   # 3 + 0.03*8 (%)
  exp: 60                        # exp_per_kill_normal(8)=round(4*8^1.3) (LEVELING §3)
ai_profile: aggressive_charger   # a heavier, slower charger; the toughest normal before the boss (AI_BEHAVIOR §5)
ai_params:                       # override only aggressive_charger-declared tunables
  aggro_radius: 6
  charge_speed_mult: 2.0
  charge_recover_s: 3.0
animation_states: [idle, walk, jump, fall, attack, hit, die]   # normal set
drop_table: drop_mob_010
flavor: "A broad-shouldered ape-thing with a back crusted in cooling coals that flare when it charges. The last real test of the fields before a novice dares the old kiln."
