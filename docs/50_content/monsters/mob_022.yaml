id: mob_022
schema: 20_schemas/monster.schema.md
references: [COMBAT_FORMULA, STATUS_EFFECTS, AI_BEHAVIOR, DROPS, ELEMENTS, LEVELING]
name: Lantern Wisp
tier: normal                     # ID slot 022 = R2 normal (ID_REGISTRY Monsters)
element: shadow                   # R2 mix is neutral/shadow (WORLD_PLAN R2)
weak_to: [arcane]                  # thematic; anti-shadow (ELEMENTS §2)
resists: [shadow]
level: 12
size_class: medium
stats:                           # copied from COMBAT_FORMULA §13 normal budget, Lv 12
  life: 900                     # 4*(12+3)^2
  power: 42                     # 6+3*12 (touch = round(0.5*power), §13.1)
  armor: 72                     # 6*12  (armor+warding sum = 144 = 12*12)
  warding: 72                     # 6*12
  precision: 48                 # 4*12
  evasion: 3.36                   # 3+0.03*12 (%)
  exp: 101                      # exp_per_kill_normal(12)=round(4*12^1.3) (LEVELING §3)
ai_profile: support_caller
ai_params:                       # override only support_caller-declared tunables (AI_BEHAVIOR)
  support_radius: 6
  cast_cooldown_s: 6
  retreat_range: 6
animation_states: [idle, walk, jump, fall, attack, hit, die]   # normal set; no telegraph/spawn
drop_table: drop_mob_022
flavor: "A cold blue mote drifting between the cellar lanterns, drawn to vermin and away from anything that swings back. It does not bite; it broods over the pack and lends them a borrowed, guttering courage."
