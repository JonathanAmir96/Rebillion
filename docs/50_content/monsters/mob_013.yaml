id: mob_013
schema: 20_schemas/monster.schema.md
references: [COMBAT_FORMULA, STATUS_EFFECTS, AI_BEHAVIOR, DROPS, ELEMENTS, LEVELING]
name: Cellar Rat
tier: normal                     # ID slot 013 = R2 normal (ID_REGISTRY Monsters)
element: neutral                   # R2 mix is neutral/shadow (WORLD_PLAN R2)
level: 8
size_class: small
stats:                           # copied from COMBAT_FORMULA §13 normal budget, Lv 8
  life: 484                     # 4*(8+3)^2
  power: 30                     # 6+3*8 (touch = round(0.5*power), §13.1)
  armor: 48                     # 6*8  (armor+warding sum = 96 = 12*8)
  warding: 48                     # 6*8
  precision: 32                 # 4*8
  evasion: 3.24                   # 3+0.03*8 (%)
  exp: 60                      # exp_per_kill_normal(8)=round(4*8^1.3) (LEVELING §3)
ai_profile: pack_hunter
ai_params:                       # override only pack_hunter-declared tunables (AI_BEHAVIOR)
  aggro_radius: 5
  pack_call_radius: 8
  pack_max_size: 5
animation_states: [idle, walk, jump, fall, attack, hit, die]   # normal set; no telegraph/spawn
drop_table: drop_mob_013
flavor: "A grey wharf-rat grown bold on Millbrook's spilled grain, no bigger than a boot and never found alone. One is a squeak in the dark; the dozen that answer it are the undervault's welcome."
