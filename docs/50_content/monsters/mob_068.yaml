id: mob_068
schema: 20_schemas/monster.schema.md
references: [COMBAT_FORMULA, STATUS_EFFECTS, AI_BEHAVIOR, DROPS, ELEMENTS, LEVELING]
name: Silkcreep Lurker
tier: normal                     # ID slot 068 = R5 Gloomwood normal (ID_REGISTRY Monsters)
element: shadow                    # R5 element mix = shadow/nature/neutral (WORLD_PLAN R5)
weak_to: [fire]
resists: [shadow]
level: 20
size_class: small
stats:                           # COMBAT_FORMULA §13 normal budget, Lv 20
  life: 2116                     # 4*(20+3)^2
  power: 66                      # 6+3*20 (touch = round(0.5*power), neutral, §13.1)
  armor: 120                      # 6*20  (armor+warding sum = 240 = 12*20)
  warding: 120                    # 6*20
  precision: 80                  # 4*20
  evasion: 3.6                    # 3 + 0.03*20 (%)
  exp: 197                       # exp_per_kill_normal(20)=round(4*20^1.3) (LEVELING §3)
ai_profile: ambush_lurker
ai_params:                       # override only ambush_lurker-declared tunables (AI_BEHAVIOR)
  ambush_radius: 2
  leash_radius: 3
  aggro_vertical_band: 1
animation_states: [idle, walk, jump, fall, attack, hit, die]   # normal set; no telegraph/spawn (elite/boss only)
drop_table: drop_mob_068
flavor: "A flat, leaf-colored spider that folds itself into a silk-lined pit and waits for a footfall overhead. The first you know of it is the ground giving way to fangs."
