id: mob_014
schema: 20_schemas/monster.schema.md
references: [COMBAT_FORMULA, STATUS_EFFECTS, AI_BEHAVIOR, DROPS, ELEMENTS, LEVELING]
name: Wharf Cur
tier: normal                     # ID slot 014 = R2 normal (ID_REGISTRY Monsters)
element: neutral                   # R2 mix is neutral/shadow (WORLD_PLAN R2)
level: 8
size_class: medium
stats:                           # copied from COMBAT_FORMULA §13 normal budget, Lv 8
  life: 484                     # 4*(8+3)^2
  power: 30                     # 6+3*8 (touch = round(0.5*power), §13.1)
  armor: 48                     # 6*8  (armor+warding sum = 96 = 12*8)
  warding: 48                     # 6*8
  precision: 32                 # 4*8
  evasion: 3.24                   # 3+0.03*8 (%)
  exp: 60                      # exp_per_kill_normal(8)=round(4*8^1.3) (LEVELING §3)
ai_profile: aggressive_charger
ai_params:                       # override only aggressive_charger-declared tunables (AI_BEHAVIOR)
  aggro_radius: 7
  charge_speed_mult: 2.5
  charge_recover_s: 2.5
animation_states: [idle, walk, jump, fall, attack, hit, die]   # normal set; no telegraph/spawn
drop_table: drop_mob_014
flavor: "A half-feral dock dog gone stray in the cellar lanes, ribs showing and hackles up. It commits to a lunge with its whole scrawny weight and takes a breath to gather itself before the next."
