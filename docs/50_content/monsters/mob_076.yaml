id: mob_076
schema: 20_schemas/monster.schema.md
references: [COMBAT_FORMULA, STATUS_EFFECTS, AI_BEHAVIOR, DROPS, ELEMENTS, LEVELING]
name: Gloomweaver Acolyte
tier: normal                     # ID slot 076 = R5 Gloomwood normal (ID_REGISTRY Monsters)
element: shadow                    # R5 element mix = shadow/nature/neutral (WORLD_PLAN R5)
weak_to: [arcane]
resists: [shadow]
level: 24
size_class: medium
stats:                           # COMBAT_FORMULA §13 normal budget, Lv 24
  life: 2916                     # 4*(24+3)^2
  power: 78                      # 6+3*24 (touch = round(0.5*power), neutral, §13.1)
  armor: 144                      # 6*24  (armor+warding sum = 288 = 12*24)
  warding: 144                    # 6*24
  precision: 96                  # 4*24
  evasion: 3.72                    # 3 + 0.03*24 (%)
  exp: 249                       # exp_per_kill_normal(24)=round(4*24^1.3) (LEVELING §3)
ai_profile: support_caller
ai_params:                       # override only support_caller-declared tunables (AI_BEHAVIOR)
  support_radius: 6
  cast_cooldown_s: 6
  retreat_range: 6
animation_states: [idle, walk, jump, fall, attack, hit, die]   # normal set; no telegraph/spawn (elite/boss only)
drop_table: drop_mob_076
flavor: "A hooded thing that tends the brood's web-shrines and mutters strength into the strands. It keeps well behind its wards, threading the dark tighter the longer it lives."
