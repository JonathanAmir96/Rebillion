id: mob_144
schema: 20_schemas/monster.schema.md
references: [COMBAT_FORMULA, STATUS_EFFECTS, AI_BEHAVIOR, DROPS, ELEMENTS, LEVELING]
name: Arclight Turret
tier: normal                     # ID_REGISTRY: mob_144 in R8 normals block 129-144
element: arcane                   # WORLD_PLAN R8 split = neutral/arcane
weak_to: [neutral]                 # ELEMENTS §2 (mutually exclusive lists)
resists: [arcane]
level: 40
size_class: large
stats:                           # COMBAT_FORMULA §13 normal budget, Lv 40
  life: 7396                     # 4*(40+3)^2
  power: 126                      # 6+3*40 (touch = round(0.5*power), §13.1)
  armor: 240                      # 6*40  (armor+warding sum = 480 = 12*40)
  warding: 240                    # 6*40
  precision: 160                  # 4*40
  evasion: 4.2                   # 3 + 0.03*40 (%)
  exp: 484                      # exp_per_kill_normal(40)=round(4*40^1.3) (LEVELING §3)
ai_profile: stationary_turret
ai_params:
  aggro_radius: 7
  attack_cooldown_s: 2.4
  windup_s: 0.7
animation_states: [idle, walk, jump, fall, attack, cast, hit, die]   # normal set; +cast (caster); no telegraph/spawn (elite/boss only)
drop_table: drop_mob_144
flavor: "A wall-set beam-caster that still sweeps its arc of stored arclight across the approach. The charge-glow is your only warning and your only mercy; it is a fixed, learnable line."
