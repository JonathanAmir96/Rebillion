id: mob_061
schema: 20_schemas/monster.schema.md
references: [COMBAT_FORMULA, STATUS_EFFECTS, AI_BEHAVIOR, DROPS, ELEMENTS, LEVELING]
name: Reef Stalker
tier: normal                     # ID slot 061 = R4 normal (ID_REGISTRY Monsters)
element: frost
weak_to: [fire]
resists: [frost]
level: 20
size_class: medium
stats:                           # all copied from COMBAT_FORMULA §13 normal budget, Lv 20 (checksum table row)
  life: 2115                     # §13 Lv 20 (4*(20+3)^2 = 2116; table checksum 2115)
  power: 66                      # 6+3*20 (touch = round(0.5*power), neutral, §13.1)
  armor: 120                     # 6*20  (armor+warding sum = 240 = 12*20)
  warding: 120                   # 6*20
  precision: 80                  # 4*20
  evasion: 3.6                   # 3 + 0.03*20 (%)
  exp: 196                       # exp_per_kill_normal(20)=round(4*20^1.3) (LEVELING §3)
ai_profile: aggressive_charger   # a cold-water barracuda that runs down anything in open water (AI_BEHAVIOR §5)
ai_params:                       # override only aggressive_charger-declared tunables
  aggro_radius: 8
  charge_speed_mult: 3.0
  charge_recover_s: 2.3
animation_states: [idle, walk, jump, fall, attack, hit, die]   # normal set; no telegraph/spawn (elite/boss only)
drop_table: drop_mob_061
flavor: "A sleek, silver reef-hunter that shadows the kelp margins and commits to a strike with terrifying speed. Sidestep the run and it overshoots, gills flaring, cold and furious."
