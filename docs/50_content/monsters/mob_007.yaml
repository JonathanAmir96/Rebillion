id: mob_007
schema: 20_schemas/monster.schema.md
references: [COMBAT_FORMULA, STATUS_EFFECTS, AI_BEHAVIOR, DROPS, ELEMENTS, LEVELING]
name: Sparkcap Skulker
tier: normal                     # ID slot 007 = R1 normal (ID_REGISTRY Monsters)
element: fire                    # 2 of R1's 3 fire normals (WORLD_PLAN R1 mix)
weak_to: [frost]                 # a smoldering fungus-thing (ELEMENTS §2; mutually exclusive lists)
resists: [fire]
level: 5
size_class: small
stats:                           # copied from COMBAT_FORMULA §13 normal budget, Lv 5
  life: 255                      # 4*(5+3)^2
  power: 21                      # 6+3*5 (touch dmg = round(0.5*power); fire body per element, §13.1)
  armor: 30                      # 6*5  (armor+warding sum = 60 = 12*5)
  warding: 30                    # 6*5
  precision: 20                  # 4*5
  evasion: 3.2                   # 3 + 0.03*5 (%)
  exp: 32                        # exp_per_kill_normal(5)=round(4*5^1.3) (LEVELING §3)
ai_profile: ambush_lurker        # waits buried and punishes stepping over it; teaches caution (AI_BEHAVIOR §7)
ai_params:                       # override only ambush_lurker-declared tunables
  ambush_radius: 2
  leash_radius: 3
  aggro_vertical_band: 1
animation_states: [idle, walk, jump, fall, attack, hit, die]   # normal set
drop_table: drop_mob_007
flavor: "A capped ember-fungus that squats in the ash passing for a harmless mushroom until a boot lands too close. Then the cap splits and a mouth of coals lunges up."
