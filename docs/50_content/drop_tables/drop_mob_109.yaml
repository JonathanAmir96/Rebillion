id: drop_mob_109
schema: 20_schemas/drop_table.schema.md
references: [DROPS, ITEMS]        # normal table: no emberstone, no pool -> no ENHANCEMENT/WORLD_PLAN/PARTY
owner: mob_109
rows:
  # shards guaranteed, normal x1. mean_shards_normal(30)=round(1.5*30+3)=48; range=[round(0.8*48),round(1.2*48)]=[38,58] (DROPS §3)
  - { ref: shards, chance: guaranteed, qty_min: 38, qty_max: 58 }
  # 1 region material, common (DROPS §5.1); item_etc_0098 is this mob's R7 material
  - { ref: item_etc_0098, chance: common, qty_min: 1, qty_max: 2 }
  # a use item (tonic) at rare (DROPS §5.1); Superior Life Tonic serves the Lv 30 band (ITEMS §1.1)
  - { ref: item_use_0004, chance: rare, qty_min: 1, qty_max: 1 }
