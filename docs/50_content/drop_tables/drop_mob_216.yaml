id: drop_mob_216
schema: 20_schemas/drop_table.schema.md
references: [DROPS, ITEMS]        # normal table: no emberstone/pool row
owner: mob_216
rows:
  # shards guaranteed. mean_shards_normal(72)=round(1.5*72+3)=111; range=[round(0.8*m),round(1.2*m)] (DROPS §3)
  - { ref: shards, chance: guaranteed, qty_min: 89, qty_max: 133 }
  # 1 region material, common (DROPS §5.1)
  - { ref: item_etc_0169, chance: common, qty_min: 1, qty_max: 2 }
  # a use item (tonic) at rare (DROPS §5.1); arc-2 Lv 66-80 tonic band
  - { ref: item_use_0019, chance: rare, qty_min: 1, qty_max: 1 }
