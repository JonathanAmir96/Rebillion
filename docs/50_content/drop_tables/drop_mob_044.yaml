id: drop_mob_044
schema: 20_schemas/drop_table.schema.md
references: [DROPS, ITEMS, ENHANCEMENT]   # elite: emberstone row -> ENHANCEMENT
owner: mob_044
rows:
  # shards guaranteed, elite x4. mean_shards_normal(14)=24; x4=96; range=[round(0.8*96),round(1.2*96)]=[77,115] (DROPS §3)
  - { ref: shards, chance: guaranteed, qty_min: 77, qty_max: 115 }
  # 2 region materials (DROPS §5.2 — elites are the material-density farm)
  - { ref: item_etc_0035, chance: common, qty_min: 1, qty_max: 2 }
  - { ref: item_etc_0047, chance: uncommon, qty_min: 1, qty_max: 2 }
  # one region-tier emberstone (DROPS §5.2, ENHANCEMENT §1). R3 gear req_level <=16 -> Emberstone I (T1-T2)
  - { ref: item_etc_0193, chance: uncommon, qty_min: 1, qty_max: 1 }
  # one pool roll (rarity_source: elite, DROPS §5.2/§5.5); pool_equip_r03 authored in parallel items batch (dangling OK)
  - { ref: pool_equip_r03, chance: guaranteed, qty_min: 1, qty_max: 1, rarity_source: elite }
  # a use item at uncommon (DROPS §5.2)
  - { ref: item_use_0011, chance: uncommon, qty_min: 1, qty_max: 1 }
