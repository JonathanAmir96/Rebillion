id: drop_mob_025
schema: 20_schemas/drop_table.schema.md
references: [DROPS, ITEMS, ENHANCEMENT]   # elite: emberstone row -> ENHANCEMENT
owner: mob_025
rows:
  # shards guaranteed, elite x4. mean=round(1.5*14+3)=24; x4=96; range=[round(0.8*96),round(1.2*96)]=[77,115] (DROPS §3)
  - { ref: shards, chance: guaranteed, qty_min: 77, qty_max: 115 }
  # 2-3 region materials (DROPS §5.2 — elites are the material-density farm)
  - { ref: item_etc_0029, chance: common, qty_min: 1, qty_max: 2 }
  - { ref: item_etc_0017, chance: uncommon, qty_min: 1, qty_max: 2 }
  # one region-tier emberstone (DROPS §5.2, ENHANCEMENT §1). R2 gear = T1-T2 -> Emberstone I
  - { ref: item_etc_0193, chance: uncommon, qty_min: 1, qty_max: 1 }
  # one pool roll (rarity_source: elite, DROPS §5.2/§5.5); pool_equip_r02 (parallel items batch)
  - { ref: pool_equip_r02, chance: guaranteed, qty_min: 1, qty_max: 1, rarity_source: elite }
  # a use item at uncommon (DROPS §5.2)
  - { ref: item_use_0002, chance: uncommon, qty_min: 1, qty_max: 1 }
