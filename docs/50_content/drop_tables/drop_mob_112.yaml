id: drop_mob_112
schema: 20_schemas/drop_table.schema.md
references: [DROPS, ITEMS]        # normal table: no emberstone, no pool -> no ENHANCEMENT/WORLD_PLAN/PARTY
owner: mob_112
rows:
  # shards guaranteed, normal x1. mean_shards_normal(32)=round(1.5*32+3)=51; range=[round(0.8*51),round(1.2*51)]=[41,61] (DROPS §3)
  - { ref: shards, chance: guaranteed, qty_min: 41, qty_max: 61 }
  # 1 region material, common (DROPS §5.1); item_etc_0101 is this mob's R7 material
  - { ref: item_etc_0101, chance: common, qty_min: 1, qty_max: 2 }
  # a use item (tonic) at rare (DROPS §5.1); Superior Life Tonic serves the Lv 32 band (ITEMS §1.1)
  - { ref: item_use_0004, chance: rare, qty_min: 1, qty_max: 1 }
