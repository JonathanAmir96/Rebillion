id: drop_mob_168
schema: 20_schemas/drop_table.schema.md
references: [DROPS, ITEMS]        # normal table: no emberstone, no pool row
owner: mob_168
rows:
  # shards guaranteed. mean_shards_normal(53)=round(1.5*53+3)=83; range=[round(0.8*83),round(1.2*83)] (DROPS §3)
  - { ref: shards, chance: guaranteed, qty_min: 66, qty_max: 100 }
  # region material(s), DROPS §5.1
  - { ref: item_etc_0142, chance: common, qty_min: 1, qty_max: 2 }
  - { ref: item_etc_0138, chance: uncommon, qty_min: 1, qty_max: 1 }
  # a use item (tonic) at rare (DROPS §5.1); Sovereign Life Tonic serves the Lv 40-band
  - { ref: item_use_0017, chance: rare, qty_min: 1, qty_max: 1 }
