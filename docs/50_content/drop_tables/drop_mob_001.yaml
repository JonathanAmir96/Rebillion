id: drop_mob_001
schema: 20_schemas/drop_table.schema.md
references: [DROPS, ITEMS]        # normal table: no emberstone/pool row -> no ENHANCEMENT/WORLD_PLAN
owner: mob_001
rows:
  # shards guaranteed. mean_shards_normal(1)=round(1.5*1+3)=5; range=[round(0.8*5),round(1.2*5)]=[4,6] (DROPS §3)
  - { ref: shards, chance: guaranteed, qty_min: 4, qty_max: 6 }
  # 1 region material, common (DROPS §5.1); item_etc_0001 is this mob's R1 material
  - { ref: item_etc_0001, chance: common, qty_min: 1, qty_max: 1 }
