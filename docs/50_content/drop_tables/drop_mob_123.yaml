id: drop_mob_123
schema: 20_schemas/drop_table.schema.md
references: [DROPS, ITEMS]        # normal table: no emberstone, no pool -> no ENHANCEMENT/WORLD_PLAN/PARTY
owner: mob_123
rows:
  # shards guaranteed, normal x1. mean_shards_normal(38)=round(1.5*38+3)=60; range=[round(0.8*60),round(1.2*60)]=[48,72] (DROPS §3)
  - { ref: shards, chance: guaranteed, qty_min: 48, qty_max: 72 }
  # 1 region material, common (DROPS §5.1); item_etc_0112 is this mob's R7 material
  - { ref: item_etc_0112, chance: common, qty_min: 1, qty_max: 2 }
  # a use item (tonic) at rare (DROPS §5.1); Prime Life Tonic serves the Lv 38 band (ITEMS §1.1)
  - { ref: item_use_0005, chance: rare, qty_min: 1, qty_max: 1 }
