id: drop_mob_068
schema: 20_schemas/drop_table.schema.md
references: [DROPS, ITEMS]        # normal table: no emberstone/pool -> no ENHANCEMENT/WORLD_PLAN
owner: mob_068
rows:
  # shards guaranteed. mean_shards_normal(20)=round(1.5*20+3)=33; range=[round(0.8*33),round(1.2*33)]=[26,40] (DROPS §3)
  - { ref: shards, chance: guaranteed, qty_min: 26, qty_max: 40 }
  # 1 region material, common (DROPS §5.1); item_etc_0065 is this mob's R5 material
  - { ref: item_etc_0065, chance: common, qty_min: 1, qty_max: 2 }
  # a use item (tonic) at rare (DROPS §5.1); Greater Life Tonic serves the Lv 19-27 band (ITEMS §1.1)
  - { ref: item_use_0003, chance: rare, qty_min: 1, qty_max: 1 }
