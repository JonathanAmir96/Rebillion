id: drop_mob_102
schema: 20_schemas/drop_table.schema.md
references: [DROPS, ITEMS]        # normal table: no emberstone/pool row
owner: mob_102
rows:
  # shards guaranteed. mean_shards_normal(33)=round(1.5*33+3)=52; range=[round(0.8*52),round(1.2*52)] (DROPS §3)
  - { ref: shards, chance: guaranteed, qty_min: 42, qty_max: 62 }
  # 1 region material, common (DROPS §5.1); item_etc_0095 is this mob's R6 material
  - { ref: item_etc_0095, chance: common, qty_min: 1, qty_max: 2 }
  # a use item (tonic) at rare (DROPS §5.1); Greater Life Tonic (Lv 30-49 band) serves this band
  - { ref: item_use_0003, chance: rare, qty_min: 1, qty_max: 1 }
