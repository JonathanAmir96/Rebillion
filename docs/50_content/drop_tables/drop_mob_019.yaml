id: drop_mob_019
schema: 20_schemas/drop_table.schema.md
references: [DROPS, ITEMS]        # normal table: no emberstone/pool -> no ENHANCEMENT/WORLD_PLAN/PARTY
owner: mob_019
rows:
  # shards guaranteed. mean_shards_normal(11)=round(1.5*11+3)=20; range=[round(0.8*20),round(1.2*20)]=[16,24] (DROPS §3)
  - { ref: shards, chance: guaranteed, qty_min: 16, qty_max: 24 }
  # 1 region material, common (DROPS §5.1); item_etc_0023 is this mob's R2 material
  - { ref: item_etc_0023, chance: common, qty_min: 1, qty_max: 2 }
  # a use item (tonic) at rare (DROPS §5.1); band-appropriate tonic for Lv 11
  - { ref: item_use_0002, chance: rare, qty_min: 1, qty_max: 1 }
