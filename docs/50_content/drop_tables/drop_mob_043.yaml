id: drop_mob_043
schema: 20_schemas/drop_table.schema.md
references: [DROPS, ITEMS]        # normal table: no emberstone/pool row
owner: mob_043
rows:
  # shards guaranteed. mean_shards_normal(16)=round(1.5*16+3)=27; range=[round(0.8*27),round(1.2*27)]=[22,32] (DROPS §3)
  - { ref: shards, chance: guaranteed, qty_min: 22, qty_max: 32 }
  # 1 region material, common (DROPS §5.1); item_etc_0048 is this mob's R3 material
  - { ref: item_etc_0048, chance: common, qty_min: 1, qty_max: 2 }
  # a use item at rare (DROPS §5.1); Life Tonic serves the Lv 8-16 band
  - { ref: item_use_0002, chance: rare, qty_min: 1, qty_max: 1 }
