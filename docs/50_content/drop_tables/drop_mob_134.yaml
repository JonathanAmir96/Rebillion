id: drop_mob_134
schema: 20_schemas/drop_table.schema.md
references: [DROPS, ITEMS]        # normal table: no emberstone/pool row
owner: mob_134
rows:
  # shards guaranteed. mean_shards_normal(36)=round(1.5*36+3)=57; range=[round(0.8*57),round(1.2*57)]=[46,68] (DROPS §3)
  - { ref: shards, chance: guaranteed, qty_min: 46, qty_max: 68 }
  # 1 region material, common (DROPS §5.1); item_etc_0118 is this mob's R8 material
  - { ref: item_etc_0118, chance: common, qty_min: 1, qty_max: 2 }
  # optional use item at rare (DROPS §5.1); Prime Tonic serves the arc-1 Lv 37-42 top band (ITEMS §1.1)
  - { ref: item_use_0005, chance: rare, qty_min: 1, qty_max: 1 }
