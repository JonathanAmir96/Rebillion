id: drop_mob_042
schema: 20_schemas/drop_table.schema.md
references: [DROPS, ITEMS]        # normal table: no emberstone/pool row
owner: mob_042
rows:
  # shards guaranteed. mean_shards_normal(15)=round(1.5*15+3)=26; range=[round(0.8*26),round(1.2*26)]=[21,31] (DROPS §3)
  - { ref: shards, chance: guaranteed, qty_min: 21, qty_max: 31 }
  # 1 region material, common (DROPS §5.1); item_etc_0047 is this mob's R3 material
  - { ref: item_etc_0047, chance: common, qty_min: 1, qty_max: 2 }
  # a use item at rare (DROPS §5.1); Antidote fits the hollow's poison flavor
  - { ref: item_use_0011, chance: rare, qty_min: 1, qty_max: 1 }
