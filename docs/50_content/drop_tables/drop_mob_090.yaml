id: drop_mob_090
schema: 20_schemas/drop_table.schema.md
references: [DROPS, ITEMS]        # normal table: no emberstone/pool row
owner: mob_090
rows:
  # shards guaranteed. mean_shards_normal(27)=round(1.5*27+3)=44; range=[round(0.8*44),round(1.2*44)] (DROPS §3)
  - { ref: shards, chance: guaranteed, qty_min: 35, qty_max: 53 }
  # 1 region material, common (DROPS §5.1); item_etc_0083 is this mob's R6 material
  - { ref: item_etc_0083, chance: common, qty_min: 1, qty_max: 2 }
  # a use item (tonic) at rare (DROPS §5.1); Life Tonic (Lv 10-29 band) serves this band
  - { ref: item_use_0002, chance: rare, qty_min: 1, qty_max: 1 }
