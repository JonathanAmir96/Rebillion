id: drop_mob_160
schema: 20_schemas/drop_table.schema.md
references: [DROPS, ITEMS]        # normal table: no emberstone, no pool row
owner: mob_160
rows:
  # shards guaranteed. mean_shards_normal(47)=round(1.5*47+3)=74; range=[round(0.8*74),round(1.2*74)] (DROPS §3)
  - { ref: shards, chance: guaranteed, qty_min: 59, qty_max: 89 }
  # region material(s), DROPS §5.1
  - { ref: item_etc_0138, chance: common, qty_min: 1, qty_max: 2 }
  # a use item (tonic) at rare (DROPS §5.1); Sovereign Life Tonic serves the Lv 40-band
  - { ref: item_use_0017, chance: rare, qty_min: 1, qty_max: 1 }
